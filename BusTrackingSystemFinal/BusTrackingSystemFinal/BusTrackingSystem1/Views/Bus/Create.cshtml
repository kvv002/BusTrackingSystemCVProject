@model BusTrackingSystem1.Models.bus

@{
    ViewBag.Title = "Create";
}

<h2 style="color: #2e5cb8">Add New</h2>

@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal" style="color:white">
        
        <hr />
        @Html.ValidationSummary(true)

        <div class="form-group">
            @Html.LabelFor(model => model.bus_type_id, "Bus Type ID", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("bus_type_id", String.Empty)
                @Html.ValidationMessageFor(model => model.bus_type_id)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.company_id, "Company ID", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("company_id", String.Empty)
                @Html.ValidationMessageFor(model => model.company_id)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
