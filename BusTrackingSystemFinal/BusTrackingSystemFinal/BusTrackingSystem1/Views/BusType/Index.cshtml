@model IEnumerable<BusTrackingSystem1.Models.bus_type>

@{
    ViewBag.Title = "Index";
}



<p style="padding-top: 60px; font-size: 20px;">
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table" style="color:white">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.bus_type_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.bus_type_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.bus_type_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.bus_type_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.bus_type_id })
        </td>
    </tr>
}

</table>
