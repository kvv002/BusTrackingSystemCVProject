@model BusTrackingSystem1.Models.bus_type

@{
    ViewBag.Title = "Edit";
}

<h2 style="color: #2e5cb8">Edit</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal" style="color:white">
       
        <hr />
        @Html.ValidationSummary(true)
        @Html.HiddenFor(model => model.bus_type_id)

        <div class="form-group">
            @Html.LabelFor(model => model.bus_type_name, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.bus_type_name)
                @Html.ValidationMessageFor(model => model.bus_type_name)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
