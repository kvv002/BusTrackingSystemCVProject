@model BusTrackingSystem1.Models.complaint

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/SearchPageLayout.cshtml";
}

<h2 style="color: #2e5cb8">Edit</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
     <div class="form-horizontal" style="color:white">
        <h4>complaint</h4>
        <hr />
        @Html.ValidationSummary(true)
        @Html.HiddenFor(model => model.complaint_id)

        <div class="form-group">
            @Html.LabelFor(model => model.complaint1, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.complaint1)
                @Html.ValidationMessageFor(model => model.complaint1)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.route_id, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.route_id)
                @Html.ValidationMessageFor(model => model.route_id)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
